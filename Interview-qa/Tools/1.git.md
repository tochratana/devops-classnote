**Git**
> What?

Git គឺជា tool សម្រាប់គ្រប់គ្រង version code។

> Why?

ដើម្បីដឹងថា code ត្រូវបានកែអ្វីខ្លះ និងអាច rollback បាន។

> When?

ប្រើរាល់ពេលសរសេរ project។

> How?
```
git add .
git commit -m "update feature"
git push origin main
```
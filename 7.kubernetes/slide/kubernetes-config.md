## Kubernetes Configuration

- Kubernetes uses YAML configuration files
- Configuration describes the desired state
- Managed by kubectl and Kubernetes API Server
- Infrastructure is declarative, not imperative

Kubernetes configuration mean, how we tell kubernetes what we want. Instead of running many commands, we write a YAML file that describes:
- យើងគួរ run app ណាខ្លះ
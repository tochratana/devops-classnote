## Note

Stateful vs stateless

- stateful : when deployment that have data store (example like postgres)
- stateless : deployment that don't have data store (example like frontend)

```bash
kubectl events
```

kubernetes operators
Vault
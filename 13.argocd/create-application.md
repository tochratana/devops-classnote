## Note

- application: any name that we want to put
- project name: default -> mean using argocd 
- Sync policy : sync auto or not with GitOps
  - if auto have two thing that we should know
    - Prune Resource : remove all resource that when user config and remove it
    - Self healing : check all update when use edit